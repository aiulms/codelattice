{
  "name": "codelattice-webui-contract-tests",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "P0-F0 characterization suite for the legacy snapshot-viewer contracts (snapshot schema, selection semantics, G6 event payloads, runner DTOs, browser smoke)",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:node": "node --test tests/snapshot-contract.test.mjs tests/graph-events.test.mjs tests/runner-dto.test.mjs",
    "test:browser": "node --test tests/viewer-smoke.test.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.50.0"
  }
}