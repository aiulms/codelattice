# CodeLattice 0.17.0-beta.1 Release Refresh Closure

Date: 2026-06-05

## Summary

Prepared CodeLattice `0.17.0-beta.1` as the next beta release candidate after `v0.16.0-beta.1`, and regenerated the local `darwin-arm64` release artifact after the release metadata commit existed so the artifact manifest points at the release commit.

## Completed

- Updated Cargo workspace product version to `0.17.0-beta.1`.
- Refreshed Cargo.lock package versions.
- Archived current Unreleased changelog entries into `## [0.17.0-beta.1] - 2026-06-05`.
- Added GitCode-facing release notes at `docs/release/0.17.0-beta.1-notes.md`.
- Updated README, English README, getting-started, release-install, release-packaging, smoke matrix, Linux/openEuler note, WebUI snapshot examples, install-release defaults, and MCP server/cache embedded version references.
- Regenerated `dist/codelattice-0.17.0-beta.1-darwin-arm64.tar.gz` and `.sha256`.
- Verified the artifact manifest contains the release commit `sourceCommit`, `version=0.17.0-beta.1`, and `profile.toolCount=49`.
- Ran release metadata and release smoke successfully:
  - `bash scripts/check-release-metadata.sh`
  - `bash scripts/package-release.sh`
  - `bash scripts/release-smoke.sh --tarball dist/codelattice-0.17.0-beta.1-darwin-arm64.tar.gz`

## Not Done

- No CodeLattice-Tool sync/promotion was performed.
- No GitCode Release page or artifact upload was performed inside the commit itself; publish via browser or GitCode UI after the commit/tag lands.
- No release tag is stored in source files; create and push `v0.17.0-beta.1` after commit verification.

## Expected Follow-up

After this commit lands, create/push tag `v0.17.0-beta.1` and publish GitCode Release `v0.17.0-beta.1` via browser or GitCode CLI/API using the release notes in `docs/release/0.17.0-beta.1-notes.md` plus the regenerated tarball/checksum.
