# breaking-change-review-fixture

A demo package for testing breaking-change review capabilities.

## API

### createClient
Create a new client instance. This is a PUBLIC API — changing its signature may break downstream consumers.

### Button
A reusable React component. Used across multiple pages.

## Internal

### internalHelper
Internal utility — not part of the public API.
