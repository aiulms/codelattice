# External API Surface Fixture

This package demonstrates public API surface patterns for testing.

## Public API

- `createClient(config)` — create a new client instance
- `PublicClient` — main client class
- `Button` — UI component

## CLI

Use `eas-demo` command to run the CLI.

## Internal

The `internal.ts` and `legacy.ts` modules are not part of the public API.
