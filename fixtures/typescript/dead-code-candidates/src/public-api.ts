// Public API — exported but NOT used internally
// Should be a candidate with public-api caution
export function publicUtility(): string {
  return "public";
}

export const PUBLIC_CONSTANT = "api-v1";

export function anotherPublicFn(): number {
  return 42;
}
