[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "sample-app"
version = "0.1.0"
description = "Portable smoke test fixture for CodeLattice Python Phase A"
requires-python = ">=3.8"
